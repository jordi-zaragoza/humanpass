{
  "name": "@humanpass/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@humanpass/shared": "workspace:*",
    "@simplewebauthn/server": "^11.0.0",
    "hono": "^4.7.0",
    "nanoid": "^5.1.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.16",
    "@cloudflare/workers-types": "^4.20250214.0",
    "@vitest/runner": "^3.2.4",
    "@vitest/snapshot": "^3.2.4",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "wrangler": "^3.109.0"
  }
}
